import ITheme from 'models/ITheme';
import COLORS from 'styles/colors';

const DARK: ITheme = {
  colors: COLORS,
  basic: {
    background: '#171A1F',
    text: '#f5f5f5',
  },
};

export default DARK;
