import ITheme from 'models/ITheme';
import COLORS from 'styles/colors';

const DARK: ITheme = {
  colors: COLORS,
  basic: {
    background: '#212121',
    text: '#f5f5f5',
  },
};

export default DARK;
