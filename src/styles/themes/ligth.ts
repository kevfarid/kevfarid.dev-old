import ITheme from 'models/ITheme';
import COLORS from 'styles/colors';

const LIGHT: ITheme = {
  colors: COLORS,
  basic: {
    background: '#f5f5f5',
    text: '#171A1F',
  },
};

export default LIGHT;
